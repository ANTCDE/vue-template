# ANT App Template (Vue)

A starter template for building apps that run inside [ANT-OS](https://os.antcde.io).

## Tech Stack

- **Vue 3** + **TypeScript 6**
- **Vuetify 4** with ANT design system (light + dark themes)
- **Vite 8** (Rolldown)
- **UnoCSS** with Vuetify 4 CSS layer compatibility
- **Vitest** + **@vue/test-utils** + **happy-dom** for component tests
- **@antcde/component-library** for shared build & test configs
- **@antcde/vue-utils** for ANT-OS integration (comms, signals, i18n sync)

## Quick Start

```bash
pnpm install
pnpm dev
```

The app runs on `http://localhost:5174` and is designed to be loaded inside ANT-OS as an iframe app.

## Project Structure

```
src/
  main.ts                    # App entry point
  App.vue                    # Root component (theme sync, tab layout)
  components/
    Main.vue                 # Demo component showing ANT-OS integration
  plugins/
    vuetify.ts               # Vuetify config (createAntVuetify factory)
    context.ts               # App context (comms, i18n, color mode)
  lang/
    language.ts              # i18n setup (auto-discovers translations)
    translations/            # Locale files (en, de, nl)
tests/
  app.test.ts                # App-level integration (testAppSetup helper)
  components/Main.test.ts    # Feature test for the Main component
vite.config.ts               # Vite config (createAntViteConfig factory)
uno.config.ts                # UnoCSS config (createAntUnoConfig factory)
vitest.config.ts             # Vitest config (createAntVitestConfig factory)
app-config.json              # ANT-OS app metadata
```

> `auto-imports.d.ts`, `components.d.ts`, and `.eslintrc-auto-import.json` are
> generated by the Vite factory on the first `pnpm dev` / `pnpm build`. They are
> git-ignored — run the dev server once after cloning so type-check and lint
> resolve the auto-imported globals.

## Shared Build Configs

This template uses factory functions from `@antcde/component-library/build/*`:

- **`createAntViteConfig(import.meta.url)`** — Vite plugins, CSS layer ordering, path aliases
- **`createAntVuetify()`** — Theme colors, component defaults, icons, light + dark themes
- **`createAntUnoConfig()`** — UnoCSS presets, Vuetify breakpoints, dark mode selector

These are opt-in and overridable — pass options to customize.

## ANT-OS Integration

The app communicates with ANT-OS through the `comms` object (from `@antcde/vue-utils`):

```ts
const { comms } = injectContext()
const { toolbar, notepad, context, connect, notifications, signal } = comms
```

- **toolbar** — control the app toolbar (title, menu items, search)
- **notepad** — open/close the notepad panel (tasks, SBS)
- **context** — reactive license/project/task context from ANT-OS
- **connect** — typed API client for ANT backend services
- **notifications** — show toast notifications
- **signal** — send/receive signals to/from ANT-OS (navigation, overlays, queries)

## Testing

Tests use the shared factory from `@antcde/component-library/test` (Vitest +
happy-dom + Vuetify/i18n pre-registered, with `@antcde/vue-utils` mocked).

```bash
pnpm test         # run once
pnpm test:watch   # watch mode
```

- `createMockAppContext()` — mock comms/connect/i18n/colorMode for the app context
- `testAppSetup(App, ctx, { provideContext })` — asserts the app is correctly wired to ANT-OS
- `mountApp(Component)` — mount a component with Vuetify + i18n already installed

See `tests/` for examples. Mock only at boundaries (context, API) — never internal functions.

## Linting & type-checking

```bash
pnpm lint         # ESLint (Antfu config)
pnpm type-check   # vue-tsc
```

## App manifest (`app-config.json`)

ANT-OS and the App Store read `app-config.json` to discover what your app declares. The file ships pre-filled with **illustrative examples** — replace or remove them for your app.

- **`$schema`** — points at the published JSON Schema (`@antcde/connect-ts/schemas/app-config.schema.json`) for editor autocomplete, hover docs, and validation.
- **`version`** *(required)* — app version (semver).
- **`title`** / **`tagline`** — name and short description surfaced in the App Store.
- **`tables`** — tables created in the project when the app is installed. Each column has a `type` (`integer`, `text`, `text-field`, `boolean`, `date`, `link`, `email`, `dropdown`, `document`, `float`, `sbscode`, `table`) plus optional `required`, `is_unique`, `is_indexed` (max 2/table), `default_value`, `options_value` (required for `dropdown`), `hint`, and `field_name`. Set `"tables": []` if your app ships none.
- **`signals.topics`** — generic pub/sub topics between apps. Each topic declares `scope` (`project`/`license`), `description`, `direction` (`send`/`receive`), and a payload `schema`. Send/receive via `comms.signal`. Remove the `signals` key if your app doesn't use topics.

Full reference: `@antcde/connect-ts` README → "App Manifest".

## Building for ANT-OS

```bash
pnpm build
```

Creates a `dist/` folder with the built app, `app-config.json`, `README.md`, and `CHANGELOG.md`, zipped for upload to the ANT App Store.
